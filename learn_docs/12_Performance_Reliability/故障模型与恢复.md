# 故障模型、可观测性与恢复

> 类型：系统基础。前置：[持久化](../04_Memory_Storage/持久化与恢复语义.md)、[通信完成语义](../07_Communication/DMA与RDMA完成语义.md)。

## 先定义失败的范围

单个请求失败、worker 退出、设备错误、网络分区、存储损坏和静默数值错误需要不同处理。超时只说明在期限内未得到预期结果，不能单独说明远端尚未执行。

训练中的一个 rank 失败可能使通信组其他成员无法继续；推理中的一个请求失败可能只需回收其 KV。恢复边界要与共享状态和依赖范围一致。

## 重试与幂等

读操作与有副作用的更新具有不同重试风险。使用请求身份、版本和提交记录可以帮助识别重复操作，但需要具体协议。把异常捕获后重新执行整个函数，可能重复计数、重复训练或重复发布。

## 从检测到恢复的状态

检测异常后需要停止不安全的新工作，确认仍在途的任务，隔离故障资源，选择相容恢复点，重建通信和资源，再验证后续执行。没有清理残留进程、端口或设备引用，可能使第二次启动出现另一类错误。

## 指标、日志与 Trace

指标适合聚合趋势，日志适合离散事件，trace 表达一次请求或 step 的时序。关联时需要统一的 request、sample、step、rank 和 policy version 等身份，但高基数标识不宜直接无界地成为每个指标的标签。

对齐不同节点时间时需要考虑时钟偏差；无法保证绝对时间一致时，可以利用事件依赖和通信边界推断因果关系。

## 保存频率的权衡

简化假设：每次保存成本 C，平均故障间隔 M，保存间隔 τ，忽略恢复与异步影响时，单位时间损失近似 $C/\tau+\tau/(2M)$。前项是保存成本，后项是故障造成的平均重做工作；对应最优间隔近似 $\sqrt{2CM}$。

该模型假设故障过程和损失结构足够简单，不能直接套到相关故障、弹性恢复和多层 Checkpoint。它的用途是明确权衡变量，而不是提供通用生产参数。

## 验证恢复而非只验证保存

最小验证应包括：完整加载、缺失 shard 被发现、错误版本被拒绝、恢复后一步计算与基线对照、重复恢复不产生额外副作用。实际故障注入需要独立可控环境。

相关：[分布式 Checkpoint](../09_Training_Systems/分布式Checkpoint与恢复.md)、[线上可观测性](../../docs/performance_analysis_guide/10_observability.md)。参考：[OSTEP Distributed Systems](https://pages.cs.wisc.edu/~remzi/OSTEP/dist-intro.pdf)。

---

[所属专题](README.md) · [百科总览](../README.md)
