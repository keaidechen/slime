# 批处理、排队与推理调度的演进

> 类型：基础机制与专题综述。前置：[Transformer](../02_Model_Architecture/Transformer与Attention基础.md)、[KV 内存系统](推理内存系统.md)。

## 服务优化的目标

TTFT 包含首 token 前的等待和执行；ITL 描述相邻输出 token 的间隔；TPOT 通常是按特定口径计算的平均每输出 token 时间。指标需声明是否包含网络、tokenization 和首 token。

吞吐是单位时间完成的工作量，goodput 进一步限制满足延迟和成功条件的有效工作。提高 batch 可能增加吞吐，也可能增加等待或单步时间。

## 从固定 batch 到迭代调度

固定 batch 把一组请求绑定执行，不同输出长度会导致完成时间差异。迭代级调度允许在生成步骤之间替换已完成请求，使执行集合随请求生命周期变化。

新的责任是维护每条请求的状态、KV 所有权、采样参数和取消行为。一个请求结束后，其资源可能部分释放、部分进入可复用缓存，不能简单按请求数推断显存。

## Prefill 与 Decode 的竞争

长 prefill 与 decode 的工作形态不同。将 prefill 分块可以在调度点安排其他工作，减少长段占用对交互延迟的影响；但分块大小影响效率、调度开销和缓存压力。

PD 分离允许两阶段分别配置资源，却需要传递状态并处理路由、带宽和阶段配比。它与 chunked prefill 是不同设计轴，可以按具体架构组合，不能写成单向替代关系。

## 队列不是免费的缓冲

稳定条件下，Little 定律 $L=\lambda W$ 将平均在系统请求数、有效到达率与平均停留时间联系起来。它不是任意过载系统的容量预测公式，也不直接给出 P99。

示意：稳定完成 20 req/s、平均停留 0.5 s，则系统平均约有 10 条请求。若持续到达率超过可完成率，又不拒绝或限流，队列将增长，原来的稳定平均不再适用。

## 缓存命中与负载均衡

将请求路由到持有前缀的 worker 可以减少重算，但可能集中负载。KV-aware routing 要同时考虑可复用状态、队列、容量和迁移成本。请求级命中率相同，也可能因命中前缀长度不同产生完全不同的收益。

## 演进对照

| 方法 | 针对的问题 | 新约束 |
|---|---|---|
| 动态/连续批处理 | 不等长请求造成执行空位 | 每请求状态管理 |
| KV 分页 | 动态容量浪费 | 块表与内核访问 |
| Prefix reuse | 重复前缀计算 | cache key、引用、淘汰 |
| Chunked prefill | 长 prefill 干扰 | 粒度与效率 |
| PD 分离 | 阶段资源耦合 | KV 传输、配比与故障 |

论文入口：[Orca](../../docs/paper_read/06_Orca与Continuous-Batching论文详解.md)、[SGLang](../../docs/paper_read/03_SGLang-RadixAttention论文详解.md)、[DistServe](../../docs/paper_read/07_DistServe与Prefill-Decode分离论文详解.md)。实现入口：[SGLang Scheduler](../../docs/sglang_code_walkthrough/02_runtime_core/01_scheduler_and_batch.md)。

## 完整推演与演进资料

- [手工推演运行时：请求队列、Token Budget、CUDA Graph 与结果返回](推演_调度批次与设备执行.md)：3个请求、token预算6；批次、Graph bucket与设备完成。
- [推理运行时演进史：从执行一个batch到管理不断变化的状态](推理运行时演进史.md)：连续批次、分页、前缀复用、分块、PD分离与执行图。


---

[所属专题](README.md) · [百科总览](../README.md)
